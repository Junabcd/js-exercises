# AI 利用記録 (問題 3.1)

## 使用した AI

Claude Code (Claude Fable 5)

## 日付

2026-08-25

## プロンプト

> 問題3.1の意図がよくわかりません。
> +,-,*.%を使用するのに、なぜ無限大などを使用するのか?無限大にNaNを足せなのか、無限大に無限大を足せなのかよくわからない。そこから考えることからスタートさせたいの?

## AI の回答概要

解答コードは受け取らず、問題の意図についての説明のみを受けた。

- この問題は「JavaScript の算術演算はエラーを投げず、代わりに `Infinity` / `-Infinity` / `NaN` という特殊な値を返す」という言語仕様 (3.2.3) を体感させるための問題である。
- 「全ての組み合わせ」とは、左辺 3 通り (`Infinity`, `-Infinity`, `NaN`) × 右辺 3 通り × 演算子 4 通り (`+`, `-`, `*`, `/`) の計 36 通りを指す。
- 実行前に `Infinity - Infinity` や `Infinity / Infinity` などの結果を予想してから確認するとよい、というヒントをもらった。
- 参考資料として MDN の `NaN` / `Infinity` のページを紹介された。

## 解答への反映

上記の説明を踏まえ、解答コードは自分で作成した。